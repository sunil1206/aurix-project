# Aurix frontend - copy to .env.local for local dev.

# Base URL for the Django API. In dev the Vite proxy handles /api,
# so leaving this empty makes axios use a relative URL.
# In production, set this to e.g. https://api.aurix.example.com
VITE_API_BASE_URL=
