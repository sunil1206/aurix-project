# ============================================================
# Aurix - Production Dependencies
# Python: 3.11+
# ============================================================

# --- Core framework ---
Django==5.0.6
djangorestframework==3.15.1

# --- Authentication ---
djangorestframework-simplejwt==5.3.1

# --- Database ---
psycopg[binary]==3.1.19          # PostgreSQL driver (production)
# SQLite is bundled with Python (development)

# --- Cache & queues ---
redis==5.0.4
django-redis==5.4.0

# --- Configuration ---
python-decouple==3.8             # 12-factor env loading

# --- HTTP / external APIs ---
httpx==0.27.0                    # Async-ready HTTP client for price feeds
yfinance==0.2.40                 # Yahoo Finance scraper for the gold spot price (no API key)

# --- Rate limiting ---
django-ratelimit==4.1.0

# --- CORS (for the React frontend) ---
django-cors-headers==4.3.1

# --- Optional AI insights ---
openai==1.30.1                   # Used by insights.engines.llm_engine; optional

# --- Production server ---
gunicorn==22.0.0
whitenoise==6.6.0                # Static file serving in container

# --- Observability ---
sentry-sdk==2.3.1

# --- Dev / test ---
pytest==8.2.1
pytest-django==4.8.0
pytest-cov==5.0.0
factory-boy==3.3.0
freezegun==1.5.1                 # Time-freeze for deterministic insight tests
